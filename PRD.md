# 智能广告基建系统 产品需求文档

## 一、版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | 3.0 |
| 创建日期 | 2026-04-16 |
| 更新日期 | 2026-04-21 |
| 文档状态 | 进行中 |

---

## 二、变更日志

| 时间 | 变更人 | 主要变更内容 |
|------|--------|--------------|
| 2026-04-17 | @咖灰 | 初版需求 |
| 2026-04-20 | @AI助手 | 细化生命周期判定逻辑、策略引擎设计、前端页面功能 |
| 2026-04-21 | @AI助手 | 基于ROI数据分析重构生命周期定义，替换旧的cost-based阈值 |

---

## 三、文档说明

### 3.1 名词解释

| 术语 | 说明 |
|------|------|
| 基建 | 广告投放中的"创建广告、复制广告、调整预算"等基础运营动作 |
| 生命周期 | 商品或广告从上线到衰退的全过程阶段 |
| ROI | Return On Investment，投资回报率 = 收入 / 成本 |
| 冷启动 | 新广告上线后能否持续消耗并产生回报的阶段 |
| 决策 | 系统输出的"应该做什么"的判断，不是实际执行动作 |
| 饱和攻击 | 短时间内大量创建广告，争取曝光的策略 |

### 3.2 术语 / 缩略词

| 缩略词 | 全称 | 说明 |
|--------|------|------|
| Product | 商品 | 指短剧/内容商品 |
| Campaign | 广告单元 | 广告投放计划 |
| ROI | Return On Investment | 投资回报率 = (d0_order_amt + d0_ad_amt) / cost |
| CTR | Click Through Rate | 点击率 |
| CPA | Cost Per Action | 每次转化成本 |
| d0_order_amt | D0订单收入 | 用户付费订单金额 |
| d0_ad_amt | D0广告收入 | 广告分成收入 |

---

## 四、需求背景

### 4.1 业务问题

当前广告投放中，"基建工作"面临三大痛点：

| 痛点 | 表现 | 影响 |
|------|------|------|
| 被动响应 | 优化师在广告效果衰减后才开始补充新广告 | 预算浪费、效果下滑 |
| 经验依赖 | 何时新建、复制多少、启用哪些素材，依赖个人经验 | 决策质量参差不齐 |
| 效率瓶颈 | 批量创建工具存在，但"创建什么"仍需人工决策 | 人效低 |

### 4.2 解决方案

智能基建系统将"人工定期批量创建"升级为"系统实时自动耕作"。

**核心定位**：系统作为**广告投放指挥官**，只输出决策，不实际执行。

**关键发现**（基于数据分析）：
- 收入 = d0_order_amt + d0_ad_amt
- **盈利标准：ROI > 40%**
- 盈利Campaign的订单收入占比 > 90%，广告只是放大器
- **72小时是判断Campaign生死的关键节点**

### 4.3 预期收益

| 指标维度 | 预期效果 | 对应目标 |
|----------|----------|----------|
| 基建效率 | 优化师人均管理广告数提升 200%+ | 提高人效 |
| 基建成功率 | 新广告渡过冷启动期成功率提升 30% | 优化投放表现 |
| 账户健康度 | 衰退期广告占比降低，成长期广告占比提升 | 优化投放表现 |
| 素材利用率 | 优质素材利用周期延长，库存周转率提升 50% | 降本增效 |

---

## 五、功能详细说明

### 5.1 模块总览

| 模块 | 功能清单 |
|------|----------|
| 生命周期判定 | 商品生命周期检测、Campaign生命周期检测、阶段说明查询、阈值配置 |
| 基建策略管理 | 策略CRUD、策略模板管理、策略匹配、冷却时间控制 |
| 决策输出 | 决策生成、决策日志存储、决策统计 |
| 诊断看板 | 健康度总览、生命周期分布、策略触发排行、效率趋势、策略执行摘要 |
| 决策日志 | 决策列表查询、决策详情查看 |

---

### 5.2 功能详细说明

#### 5.2.1 生命周期判定

**功能描述**：根据广告实体的ROI数据，自动判断其所处的生命周期阶段。

**核心指标**：
```
收入 = d0_order_amt + d0_ad_amt
ROI = 收入 / 成本
盈利标准 = ROI > 40%
```

**关键数据发现**：
- 72小时是判断Campaign生死的关键节点
- 前24h ROI < 10% 的Campaign，85%最终无法盈利
- 盈利Campaign的订单收入占比 > 90%
- 49%的存活>7天的Campaign能盈利

**Campaign生命周期阶段（基于ROI）**：

| 阶段 | 判定条件 | 数据依据 | 精确率 | 运营建议 |
|------|----------|----------|--------|----------|
| 待观察 | 投放 < 24h | 时间不足，无法判断 | 0.50 | 等待 |
| 冷死亡 | 投放 > 72h 且从未产生收入 | 24.3%的Campaign从未产生收入 | 0.95 | 直接放弃 |
| 冷启动 | 前24h ROI < 10% | 前24h低ROI的85%最终不盈利 | 0.85 | 观察，24h决策 |
| 验证期 | 24-72h ROI在10-40% | 关键决策点，过了的30%能盈利 | 0.75 | 持续观察 |
| 成长期 | 72h后 ROI > 40% | 92%的这类Campaign保持盈利 | 0.85 | 加预算 |
| 持续盈利 | ROI > 40% 超过7天 | 97%最终盈利 | 0.90 | 重点维护 |
| 衰退期 | ROI从高点下降 > 50% | 后期ROI显著下降 | 0.85 | 准备替代 |
| 关停期 | ROI < 10% 持续72h+ | 几乎无法翻盘 | 0.90 | 关停 |

**商品（ShortPlay）生命周期阶段（基于回测数据验证）**：

判定逻辑基于时间段 + ROI指标组合，时间段划分如下：

| 时间段 | 阶段 | 判定条件 | 精确率 |
|--------|------|----------|--------|
| < 3天 | 待观察 | 时间不足，无法判断 | - |
| 3-7天 | 入场期 | 近3天ROI均值 > 40% | 87% |
| 3-7天 | 退出期 | 近3天ROI均值 < 10% | 85% |
| 7-14天 | 衰退期 | ROI下滑 > 30% | 80% |
| 7-14天 | 成长期 | ROI > 40% 且近3天均值 > 前3天均值 × 1.2（上升20%） | 87% |
| 7-14天 | 稳定期 | ROI > 40% 但趋势平稳 | 86% |
| > 14天 | 退出期 | ROI < 10% 持续5天 | 85% |
| > 14天 | 衰退期 | ROI下滑 > 30% | 80% |
| > 14天 | 成长期 | ROI > 40% 且趋势上升 | 87% |
| > 14天 | 稳定期 | 近5天ROI都在30%-80% | 86% |

**趋势指标定义**：

`recent_roi_history` 是每日 ROI 序列（oldest-first，最早日期在 index 0）。所有指标均基于此序列计算。

| 指标 | 计算 | 含义 |
|------|------|------|
| 近3天ROI均值 | `avg(h[-3:])` | 当前最近的投放表现 |
| 前3天ROI均值 | `avg(h[-6:-3])` | 上一段基准，用于对比趋势 |
| 前半段均值 | `avg(h[:mid])` | 历史前半段的表现水平 |
| 后半段均值 | `avg(h[mid:])` | 历史后半段的表现水平 |
| 趋势上升 | `近3天 > 前3天 × 1.2` | 近期显著改善（+20%），→ GROWTH |
| 趋势下滑 | `(前半段 - 后半段) / 前半段 > 30%` | 整体产出持续走弱，→ DECLINE |
| 稳定波动 | 全部近5天 ROI ∈ [30%, 80%] | 无显著趋势，盈利区间内震荡，→ SUSTAINED |

**Campaign 维度的类趋势指标**（基于分段 ROI 而非日序列）：

| 指标 | 计算 | 含义 |
|------|------|------|
| 后期走强 | `roi_72plus > roi_0_24h × 1.5` | 72h后ROI显著优于前24h，→ GROWTH |
| 后期走弱 | `roi_72plus < roi_0_24h × 0.5` | 72h后ROI衰减至早期一半以下，→ DECLINE |
| 从峰值跌落 | `roi < peak × 0.5` | 当前ROI不足峰值一半，→ DECLINE |

### Campaign 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| entity_id | string | Campaign唯一标识 |
| duration_hours | float | 投放时长（小时） |
| revenue | float | 总收入 (d0_order_amt + d0_ad_amt) |
| cost | float | 总成本 |
| revenue_0_24h | float | 前24小时收入 |
| cost_0_24h | float | 前24小时成本 |
| revenue_24_72h | float | 24-72小时收入 |
| cost_24_72h | float | 24-72小时成本 |
| revenue_72plus | float | 72小时后收入 |
| cost_72plus | float | 72小时后成本 |
| order_amt | float | 订单收入 (d0_order_amt) |
| ad_amt | float | 广告收入 (d0_ad_amt) |

### Campaign 输出参数

| 参数 | 类型 | 说明 |
|------|------|------|
| entity_id | string | Campaign唯一标识 |
| stage | string | 判定阶段 |
| confidence | float | 判定置信度 |
| reason | string | 判定原因说明 |
| roi | dict | ROI相关指标 (total, roi_0_24h, is_profitable) |
| profitability_probability | float | 盈利概率预测 |

### Product 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| entity_id | string | 商品唯一标识 |
| total_revenue | float | 总收入 |
| total_cost | float | 总成本 |
| campaign_count | int | 关联Campaign数量 |
| duration_hours | float | 最大投放时长（小时） |
| order_amt | float | 订单收入 |
| ad_amt | float | 广告收入 |
| recent_roi_history | list[float] | 近N天的每日ROI（oldest-first） |

### Product 输出参数

| 参数 | 类型 | 说明 |
|------|------|------|
| entity_id | string | 商品唯一标识 |
| stage | string | 判定阶段 |
| confidence | float | 判定置信度 |
| reason | string | 判定原因说明 |
| roi | dict | ROI相关指标 (total, is_profitable) |
| revenue_breakdown | dict | 收入构成 (order_amt, ad_amt, order_ratio) |

**原型/示意图**：

```
┌─────────────────────────────────────────────────────────────┐
│  生命周期判定 (基于ROI)                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入：entity_id=campaign_001                               │
│       duration_hours=48                                     │
│       revenue=20, cost=100                                  │
│       revenue_0_24h=5, cost_0_24h=100                      │
│       revenue_24_72h=15, cost_24_72h=100                   │
│                                                             │
│  ────────────────────────────────────────────────────────   │
│                                                             │
│  计算：ROI = 20/100 = 20%                                  │
│       ROI_0_24h = 5/100 = 5%                              │
│       ROI_24_72h = 15/100 = 15%                            │
│                                                             │
│  输出：stage=campaign_verify                                │
│       confidence=0.75                                       │
│       reason="验证期: ROI=20% (10%-40%)，关键决策点"      │
│       roi.total=0.20                                        │
│       roi.is_profitable=false                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

#### 5.2.2 基建策略管理

**功能描述**：配置"什么阶段触发什么动作"的策略，支持前端页面可视化配置。

**策略结构**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 策略名称 | string | 是 | 自定义名称，如"冷启动补量策略" |
| 描述 | string | 否 | 策略用途说明 |
| 维度 | enum | 是 | product / campaign |
| 触发阶段 | list | 是 | 可多选 |
| 执行动作 | enum | 是 | 具体动作类型 |
| 执行规模 | int | 是 | 每次触发生成的广告数量上限 |
| 冷却时间 | int | 否 | 默认24小时，同一实体需等待N小时才能再次触发 |
| 额外条件 | list | 否 | 除了阶段外还需满足的指标条件 |
| 状态 | bool | 否 | 默认启用 |

**支持的执行动作（基于ROI分析）**：

| 动作 | 代码 | 说明 | 适用阶段 |
|------|------|------|----------|
| 饱和式攻击 | GROWTH_BURST | 短时间内大量创建广告 | 冷死亡 |
| 增加预算 | INCREASE_BUDGET | 提高预算扩大规模 | 成长期、持续盈利 |
| 渠道扩张 | CHANNEL_EXPAND | 将成功的商品+素材复制到其他渠道 | 成长期、持续盈利 |
| 复制广告 | CLONE_AD | 复制现有广告进行测试 | 冷启动 |
| 素材预热 | MATERIAL_PREPARE | 用小预算测试新素材潜力 | 验证期 |
| 预算平滑 | BUDGET_SMOOTH | 调整不同广告间的预算分配 | 验证期 |
| 维持现状 | MAINTAIN | 保持当前投放策略 | 持续盈利 |
| 有序关停 | GRACEFUL_SHUTDOWN | 逐步降低预算后关停 | 衰退期、关停期 |
| 基建补充 | REBUILD | 用新素材重新创建广告 | 衰退期、亏损 |
| 降低预算 | REDUCE_BUDGET | 减少预算开支 | 衰退期 |

**预置策略模板（11个）**：

| 模板名称 | 维度 | 触发阶段 | 执行动作 | 规模 | 冷却 |
|----------|------|----------|----------|------|------|
| 冷死亡-饱和攻击 | Campaign | 冷死亡 | 饱和式攻击 | 50 | 24h |
| 冷启动-复制替换 | Campaign | 冷启动 | 复制广告 | 10 | 12h |
| 验证期-素材预热 | Campaign | 验证期 | 素材预热 | 5 | 48h |
| 成长期-增加预算 | Campaign | 成长期 | 增加预算 | 20% | 72h |
| 持续盈利-渠道扩张 | Campaign | 持续盈利 | 渠道扩张 | 20 | 168h |
| 衰退期-有序关停 | Campaign | 衰退期 | 有序关停 | 1 | 24h |
| 衰退期-基建补充 | Campaign | 衰退期 | 基建补充 | 30 | 72h |
| 关停期-停止投放 | Campaign | 关停期 | 有序关停 | 1 | 0h |
| 盈利商品-扩张 | Product | 盈利 | 渠道扩张 | 20 | 168h |
| 亏损商品-重建 | Product | 亏损 | 基建补充 | 30 | 72h |
| 无收入商品-放弃 | Product | 无收入 | 有序关停 | 1 | 0h |

**原型/示意图**：

```
┌─────────────────────────────────────────────────────────────────────┐
│  策略配置页面                                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┐  ┌─────────────────────────────────────┐   │
│  │ 策略列表         │  │ 策略模板                            │   │
│  ├─────────────────┤  ├─────────────────────────────────────┤   │
│  │ ☑ 冷死亡饱和攻击 │  │ [模板1] 冷死亡-饱和攻击            │   │
│  │ ☑ 成长期加预算   │  │ [模板2] 成长期-增加预算            │   │
│  │ ☑ 衰退期补充    │  │ [模板3] 衰退期-基建补充            │   │
│  │ ☐ Product策略    │  │ ...                                 │   │
│  └─────────────────┘  └─────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  添加策略                                                    │ │
│  │  ─────────────────────────────────────────────────────────  │ │
│  │  策略名称: [冷启动复制策略___________]                       │ │
│  │  维度:      ( )商品  (●)广告单元                             │ │
│  │  触发阶段:  [冷启动 ×]                                     │ │
│  │  执行动作:  [复制广告 ▼]                                     │ │
│  │  规模:      [10____]                                       │ │
│  │  冷却时间:  [12____] 小时                                   │ │
│  │  ─────────────────────────────────────────────────────────  │ │
│  │                                     [取消]  [保存]           │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**页面布局说明**：

策略管理页面采用左宽右窄的经典布局：

```
┌──────────────────────────────┐  ┌──────────────┐
│  策略列表 (16/24)             │  │  策略模板      │
│  ┌─────────────────────────┐ │  │  ┌──────────┐ │
│  │ 筛选维度 │ [+添加策略]    │ │  │  │ 模板卡片1  │ │
│  ├─────┬────┬────┬────┬───┤ │  │  ├──────────┤ │
│  │名称  │维度 │阶段│动作│操作│ │  │  │ 模板卡片2  │ │
│  ├─────┼────┼────┼────┼───┤ │  │  ├──────────┤ │
│  │ ...策略行...             │ │  │  │ ...       │ │
│  └─────────────────────────┘ │  │  └──────────┘ │
│                              │  ├──────────────┤
│  最近决策预览                 │  │  策略统计      │
│  ┌─────────────────────────┐ │  │  总策略 启用中 │
│  │ 时间 │实体ID│阶段│动作│规模│ │  │  今日触发     │
│  │ ...决策行...             │ │  │  本周决策     │
│  └─────────────────────────┘ │  ├──────────────┤
│                              │  │  阶段说明      │
│                              │  │  ▼ 商品维度   │
│                              │  │  ▶ 广告维度   │
└──────────────────────────────┘  └──────────────┘
```

**左侧主区域**：

| 区域 | 内容 | 交互 |
|------|------|------|
| 策略列表表格 | 名称（含启用/禁用标签）、维度、触发阶段、执行动作、规模、操作按钮 | 按维度筛选、添加策略、编辑、删除 |
| 最近决策预览 | 最近 5 条决策：时间、实体ID、阶段、动作、规模 | 点击跳转决策日志页 |

**右侧辅助区域**：

| 区域 | 内容 | 交互 |
|------|------|------|
| 策略模板 | 6 个预置模板卡片，展示名称和描述 | 点击自动填入表单 |
| 策略统计 | 总策略数、启用中、今日触发次数、本周决策数 | 只读 |
| 阶段说明 | 折叠面板：商品维度 / 广告单元维度的阶段定义 | 展开折叠 |

**添加/编辑对话框**：

| 字段 | 控件 | 说明 |
|------|------|------|
| 策略名称 | 文本输入 | 必填 |
| 描述 | 多行文本 | 选填 |
| 维度 | 下拉选择 | 商品 / 广告单元，切换后联动触发阶段可选列表 |
| 触发阶段 | 多选下拉 | 分组显示（商品阶段 / Campaign阶段），根据维度联动 |
| 执行动作 | 下拉选择 | 分组显示（增长期 / 稳定期 / 衰退期动作），含中文说明 |
| 执行规模 | 数字输入 | 1 ~ maxLimit |
| 冷却时间 | 数字输入 | 0~168 小时 |
| 额外条件 | 动态行 | 字段 + 操作符 + 阈值，可增删行 |
| 状态 | 开关 | 启用/禁用 |

**典型交互流**：

1. **新建策略**：点击"添加策略" → 填写表单 → 保存 → 策略列表刷新
2. **应用模板**：点击右侧模板卡片 → 表单自动填入模板配置 → 用户修改 → 保存
3. **编辑策略**：点击行内"编辑" → 弹出预填对话框 → 修改 → 保存
4. **启用/禁用**：表格内 tag 展示当前状态（后续可加行内 toggle）
5. **删除策略**：点击"删除" → 确认 → 从列表移除

---

#### 5.2.3 决策输出

**功能描述**：将策略匹配结果转换为可执行的决策，并存储到日志文件。

**决策格式**：

```json
{
    "timestamp": "2026-04-21T10:30:00",
    "entity_id": "campaign_001",
    "dimension": "campaign",
    "stage": "campaign_cold_dead",
    "action": "GROWTH_BURST",
    "scale": 50,
    "confidence": 0.855,
    "reason": "阶段campaign_cold_dead触发策略冷死亡-饱和攻击"
}
```

**存储位置**：

```
logs/decisions/YYYY-MM-DD/
├── decisions.jsonl           # 每日决策汇总（追加写入）
└── decision_HHMMSS.json      # 单条决策详情
```

**决策统计指标**：

| 指标 | 说明 |
|------|------|
| 今日决策数 | 当日生成的决策总条数 |
| 自动化基建计划 | 系统自动生成的广告计划数量 |
| 动作分布 | 各种动作类型的执行次数 |

---

#### 5.2.4 诊断看板

**功能描述**：投放健康度总览，一眼看清全貌。顶部指标卡 → 中部图表 → 底部告警与摘要，逐层下钻。

**页面布局**：

```
┌──────────────────────────────────────────────────────────────┐
│  诊断看板                                                      │
│                                                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐            │
│  │总商品│ │活跃  │ │今日  │ │盈利  │ │基建计划│            │
│  │1,147 │ │20,000│ │45   │ │27%  │ │68    │            │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘            │
│                                                              │
│  ┌──────────────────────────────────────────┐               │
│  │ 生命周期分布    ○广告 ○商品                │               │
│  │   [饼图]                  [阶段参考表]     │               │
│  │   冷死亡24% 冷启动40% 验证20% 成长8%       │               │
│  └──────────────────────────────────────────┘               │
│                                                              │
│  ┌────────────────────┐ ┌────────────────────┐               │
│  │ ROI分布            │ │ 策略触发排行        │               │
│  │   [柱状图]         │ │   [柱状图]         │               │
│  │ ROI>40% ████ 5,463│ │ 饱和攻击 ██ 20     │               │
│  └────────────────────┘ └────────────────────┘               │
│                                                              │
│  ┌────────────────────┐ ┌────────────────────┐               │
│  │ 基建效率趋势(7天)   │ │ 策略执行摘要         │               │
│  │ [柱 + 折线混合]    │ │ 启用策略: 8/11      │               │
│  │                    │ │ 今日触发: 12         │               │
│  │                    │ │ 本周决策: 156        │               │
│  │                    │ │ ██████ 饱和攻击 20   │               │
│  └────────────────────┘ └────────────────────┘               │
└──────────────────────────────────────────────────────────────┘
```

**区域一：概览卡片**（5 个指标卡，一行）

| 卡片 | 指标 | 口径 | 视觉 |
|------|------|------|------|
| 总商品数 | 当前在投商品数 | Hive 聚合 | 蓝色图标 |
| 活跃广告 | 当前在投 Campaign 数 | Hive 聚合 | 绿色图标 |
| 今日决策 | 当日系统生成的决策条数 | 决策日志 | 橙色图标 |
| 盈利率 | ROI > 40% 的 Campaign 占比 | 生命周期判定 | 红色图标 |
| 自动化基建计划 | 系统自动生成的广告计划总数 | 决策日志 | 灰色图标 |

**区域二：生命周期分布**（全宽，一行）

| 图表 | 类型 | 内容 | 交互 |
|------|------|------|------|
| 生命周期分布 | 环形饼图 + 阶段参考表 | 饼图展示各阶段占比，右侧表格列出阶段名称、判定条件、精确占比 | 切换商品/广告维度 |

**区域三：图表区**（2×2 排列）

| 图表 | 类型 | 内容 | 交互 |
|------|------|------|------|
| ROI 分布 | 柱状图 | 按 ROI 区间分段统计 Campaign 数量：ROI>40%、20-40%、0-20%、≤0 | 悬停展示精确数值 |
| 策略触发排行 | 柱状图 | 今日各策略触发次数排名 | — |
| 基建效率趋势 | 双轴图 | 柱=每日决策数，折线=自动化基建计划数，近 7 天 | 悬停展示每日明细 |
| 策略执行摘要 | 指标 + 进度条 | 启用策略数 x/y、今日触发次数、本周决策总数、今日动作分布 | 点击"管理策略"跳转策略页 |

**页面加载顺序**：

1. 首次加载：全用当前实现的 mock 数据渲染
2. 后端对接后：`GET /api/dashboard/summary` 获取指标卡数据，`GET /api/dashboard/lifecycle/distribution` 获取饼图数据，`GET /api/automation/decisions/stats` 获取决策统计

---

#### 5.2.5 决策日志

**功能描述**：查看系统输出的所有决策记录，支持列表浏览和详情查看。

**页面布局**：

```
┌──────────────────────────────────────────────────────────────┐
│  决策日志                                          [刷新]     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  时间              │类型      │维度    │目标ID  │动作    │详情│
│  ───────────────────────────────────────────────────────────│
│  2026-04-21 10:30 │创建广告  │campaign│camp_001│饱和攻击│查看│
│  2026-04-21 09:15 │复制广告  │campaign│camp_123│复制广告│查看│
│  2026-04-21 08:00 │调整预算  │product │prod_456│增加预算│查看│
│  ───────────────────────────────────────────────────────────│
│                                 共 1,234 条  < 1 2 3 ... 62 >│
│                                                              │
│  ┌── 决策详情弹窗 ──────────────────────────────────┐       │
│  │  {                                                │       │
│  │    "decision_id": "202604211030_campaign_001",     │       │
│  │    "timestamp": "2026-04-21T10:30:00",             │       │
│  │    "type": "CREATE_AD",                            │       │
│  │    "dimension": "campaign",                        │       │
│  │    "target_id": "campaign_001",                    │       │
│  │    "action": "GROWTH_BURST",                       │       │
│  │    "payload": { "scale": 50 },                     │       │
│  │    "confidence": 0.85,                             │       │
│  │    "reason": "阶段campaign_cold_dead触发策略..."    │       │
│  │  }                                                │       │
│  └────────────────────────────────────────────────────┘       │
└──────────────────────────────────────────────────────────────┘
```

**列表字段**：

| 列 | 内容 | 展示形式 |
|----|------|----------|
| 时间 | 决策生成时间 | 格式化字符串 |
| 决策类型 | CREATE_AD / CLONE_AD / PAUSE_AD / STOP_AD / ADJUST_BUDGET | 彩色 tag |
| 维度 | campaign / product | 文本 |
| 目标 ID | 被操作的实体标识 | 文本，超长截断 |
| 动作 | GROWTH_BURST / CHANNEL_EXPAND / ... | 彩色 tag（颜色同策略页动作映射） |
| 详情 | 查看完整 JSON | 按钮 → 弹窗 |

**详情弹窗**：点击"查看"后弹出，以 `JSON.stringify` 格式化展示完整决策对象，便于调试和排查。

**分页**：服务端分页，每页 20 条，显示总数和页码。

**刷新**：点击刷新按钮重新拉取最新数据。

**数据来源**：`GET /api/automation/decisions/stats` 的 `recent_decisions` 字段（当前 mock 数据直接渲染）。

---

### 5.3 系统运行流程

#### 5.3.1 整体流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                         完整业务流程                                  │
└─────────────────────────────────────────────────────────────────────┘

Step 1: 数据输入
─────────────────────────────────────────
Hive数仓 ──定时拉取──▶ 解析聚合 ──▶ ROI指标计算
                                       │
                                       ▼
Step 2: 生命周期判定（基于ROI）
─────────────────────────────────────────
                                   ROI指标
                                      │
                              ┌───────┴───────┐
                              ▼               ▼
                        商品维度         Campaign维度
                              │               │
                        按ROI阈值判断      按ROI阈值判断
                              │               │
                              └───────┬───────┘
                                      ▼
                              阶段 + 置信度 + 盈利概率

Step 3: 策略匹配
─────────────────────────────────────────
                              当前阶段
                                      │
                              ┌───────┴───────┐
                              ▼               ▼
                         查询策略         条件过滤
                              │               │
                              │         冷却时间检查
                              │               │
                              │        额外条件检查
                              └───────┬───────┘
                                      ▼
                               匹配的策略

Step 4: 决策输出
─────────────────────────────────────────
                              匹配的策略
                                      │
                              生成决策JSON
                                      │
                              写入日志文件
                                      │
                              看板展示统计
```

#### 5.3.2 策略匹配逻辑

```
输入：entity_id, dimension, current_stage, metrics

1. 查询所有启用的策略

2. 维度过滤
   └── dimension == strategy.dimension

3. 阶段过滤
   └── current_stage in strategy.trigger_stages

4. 冷却时间检查
   └── 当前时间 - 上次触发时间 >= cooldown_hours

5. 额外条件检查（可选）
   └── metrics 满足 strategy.conditions
```

#### 5.3.3 典型业务场景

**场景A：Campaign冷死亡**

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 系统检测Campaign 总收入=0 | 触发条件 |
| 2 | 判定阶段=冷死亡 | 置信度0.95 |
| 3 | 匹配策略=冷死亡-饱和攻击 | 规模50 |
| 4 | 输出决策 | 创建50条广告 |
| 5 | 优化师确认执行 | 实际创建 |

**场景B：Campaign进入验证期**

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 系统检测 24-72h ROI=25% | 在10-40%区间 |
| 2 | 判定阶段=验证期 | 置信度0.75 |
| 3 | 匹配策略=验证期-素材预热 | 规模5 |
| 4 | 输出决策 | 预热5个新素材 |

**场景C：Campaign进入成长期**

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 系统检测 72h后 ROI=55% > 40% | 触发条件 |
| 2 | 判定阶段=成长期 | 置信度0.85 |
| 3 | 匹配策略=成长期-增加预算 | 规模20% |
| 4 | 同时匹配=持续盈利-渠道扩张 | 规模20 |
| 5 | 输出决策 | 增加20%预算 + 扩张20条 |

---

## 六、非功能需求

### 6.1 性能需求

| 指标 | 要求 | 说明 |
|------|------|------|
| API响应时间 | < 200ms | 单次生命周期检测 |
| 批量处理能力 | 支持1000+实体/次 | 批量检测接口 |
| 日志写入 | < 50ms/条 | 决策日志写入 |

### 6.2 可用性需求

| 指标 | 要求 | 说明 |
|------|------|------|
| 系统可用性 | 99.9% | 全年停机时间 < 8.7小时 |
| 数据准确率 | > 95% | 生命周期判定准确率 |

### 6.3 可扩展性需求

| 指标 | 要求 | 说明 |
|------|------|------|
| 策略数量上限 | 不限 | 支持任意数量策略 |
| 新维度支持 | 可扩展 | 预留素材维度接入 |
| 新动作支持 | 可扩展 | 预留更多动作类型 |

### 6.4 数据需求

大数据侧提供 Campaign 粒度的按小时聚合数据，每条广告在每个小时有一条记录。

**原始字段**

| 字段 | 类型 | 说明 | 来源 |
|------|------|------|------|
| `campaign_id` | string | 广告系列 ID | 投放系统 |
| `hour` | datetime | 小时级时间戳 | 投放系统 |
| `cost_h` | float | 小时消耗（元） | 计费系统 |
| `show_cnt` | float | 展示量 | 投放系统 |
| `click_cnt` | float | 点击量 | 投放系统 |
| `belong_h_cnt` | float | 归因用户数（注册/激活） | 归因系统 |
| `belong_pay_cnt` | float | 付费用户数 | 支付系统 |
| `vip_pay_cnt` | float | 订阅用户数 | 订阅系统 |
| `d0_order_amt` | float | 当日收入（元） | 支付系统 |
| `business_type` | int | 业务类型（1=付费, 2=免费） | 投放系统 |
| `link_language` | string | 广告语言 | 投放系统 |

**聚合指标（本系统计算）**

以上游原始数据为基础，系统按 Campaign 维度聚合以下派生指标：

| 指标 | 计算方式 | 用途 |
|------|----------|------|
| 总收入 (revenue) | `SUM(d0_order_amt)` | Campaign ROI 计算 |
| 总成本 (cost) | `SUM(cost_h)` | ROI 分母 |
| ROI | `revenue / cost` | 生命周期判定核心指标 |
| 投放时长 (duration) | `MAX(hour) - MIN(hour)` | 分段判定依据 |
| 分段收入 | 按 0-24h / 24-72h / 72h+ 汇总 | SEGMENT 判定 |
| 分段成本 | 同上 | SEGMENT 判定 |
| 订单收入占比 | `SUM(d0_order_amt) / revenue` | 判定盈利质量 |

**数据质量要求**

| 指标 | 要求 | 说明 |
|------|------|------|
| 数据延迟 | < 1小时 | 上游 Hive 表就绪延迟 |
| 数据完整性 | 无缺小时 | cost_h > 0 的 campaign 必须条条有记录 |
| 历史回溯 | 至少 14 天 | 支持商品维度趋势计算 |
| 增量更新 | 按小时追加 | 上游分区表按 `hour` 分区 |

---

### 6.5 商品维度数据需求

大数据侧还需提供商品维度的按日聚合数据，以 Campaign 小时数据为基础，按 `product_id` 汇总。每个商品每天一条记录。

**原始字段**

| 字段 | 类型 | 说明 | 来源 |
|------|------|------|------|
| `product_id` | string | 商品 ID（短剧 ID） | 投放系统 |
| `date` | date | 日期 | 投放系统 |
| `cost` | float | 当日总消耗（元） | 计费系统 |
| `order_amt` | float | 当日订单收入（元） | 支付系统 |
| `ad_amt` | float | 当日广告收入（元） | 广告系统 |
| `campaign_count` | int | 当日关联投放的 Campaign 数 | 投放系统 |
| `first_campaign_hour` | datetime | 首个 Campaign 投放时间 | 投放系统 |

**聚合指标（本系统计算）**

| 指标 | 计算方式 | 用途 |
|------|----------|------|
| 总收入 (revenue) | `SUM(order_amt + ad_amt)` | 商品 ROI 计算 |
| 总成本 | `SUM(cost)` | ROI 分母 |
| ROI | `revenue / cost` | 商品盈利能力判定 |
| 投放天数 | `DATEDIFF(MAX(date), MIN(date))` | 分段判定依据 |
| 每日 ROI 序列 | 按日排列 `[d1_roi, d2_roi, ...]` | 趋势分析（上升/下滑/平稳） |
| 近3天 ROI 均值 | `AVG(last 3 days roi)` | 入场/退出判定 |
| 战役数 | `MAX(campaign_count)` | 判断投放规模 |

**数据质量要求**

| 指标 | 要求 | 说明 |
|------|------|------|
| 数据延迟 | < 1小时 | 当日数据在次日 0 点前就绪 |
| 回填历史 | 至少 14 天 | 支持 `recent_roi_history` 最近 14 天趋势计算 |
| 无成本过滤 | 不提供 cost=0 的商品 | 无投放商品无需纳入判定 |
| 增量更新 | 按日追加 | 上游分区表按 `date` 分区 |
| 实时性 | 分钟级 | 看板数据刷新间隔 |

### 6.5 安全需求

| 指标 | 要求 | 说明 |
|------|------|------|
| 权限控制 | 读/写分离 | 只读用户不能修改策略 |
| 数据脱敏 | 必须 | 日志中ID脱敏处理 |
| 操作审计 | 必须 | 记录策略修改操作 |

---

## 七、附录

### 7.1 ROI阈值配置（基于数据分析）

| 维度 | 参数 | 值 | 验证结论 |
|------|------|-----|----------|
| **盈利标准** | ROI > 40% | 27.3% Campaign能盈利 | 两周数据一致 |
| 冷启动失败 | 前24h ROI < 10% | 85%最终不盈利 | 数据验证 |
| 验证期 | 24-72h ROI 10-40% | 30%最终能盈利 | 关键决策点 |
| 成长期 | 72h后 ROI > 40% | 92%保持盈利 | 数据验证 |
| 持续盈利 | ROI > 40% 超过7天 | 97%最终盈利 | 数据验证 |
| 关停期 | ROI < 10% 持续72h+ | 几乎无法翻盘 | 数据验证 |

### 7.2 ROI与时长关系

| 上线时长 | 中位ROI | ROI>40%比例 | 特征 |
|---------|---------|-------------|------|
| 1-6h | 0% | 5.9% | 冷启动期 |
| 7-12h | 3.1% | 12.4% | 验证期 |
| 13-24h | 9.9% | 23.1% | 关键决策点 |
| 25-48h | 6.1% | 33.3% | 分化期 |
| 49-72h | 37.9% | 47.5% | **拐点** |
| 3-7d | 25.5% | 40.3% | 成熟期 |
| >7d | 39.1% | **49.0%** | 长期存活 |

### 7.3 收入构成分析

| 收入类型 | 占比 | 说明 |
|----------|------|------|
| d0_order_amt (订单收入) | 77.9% | 用户付费，是核心收入 |
| d0_ad_amt (广告收入) | 22.1% | 广告分成，辅助收入 |

**关键发现**：盈利Campaign的订单收入占比 > 90%，广告只是放大器，内容本身是否吸引用户付费才是核心。

### 7.4 数据分析样本

| 数据集 | 时间范围 | 商品数 | Campaign数 |
|--------|----------|--------|------------|
| 样本1 | 0301-0307 | 1,147 | 20,000 |
| 样本2 | 0315-0321 | 1,194 | 20,000 |

---

## 八、版本对比

### 8.1 生命周期定义对比

| 版本 | 问题 | 新版（v3.0） |
|------|------|--------------|
| 冷启动失败：cost<50元 | 缺乏ROI依据 | 前24h ROI < 10% |
| 成长期：cost_change>+20% | 基于成本，非收入 | 72h后 ROI > 40% |
| 成熟期：cost稳定 | 未考虑ROI | ROI 40%+ 稳定 |
| 衰退期：cost下降>30% | 基于成本，非收入 | ROI从高点下降>50% |

### 8.2 核心指标变化

| 指标 | 旧版 | 新版（v3.0） |
|------|------|--------------|
| 收入定义 | 未明确 | d0_order_amt + d0_ad_amt |
| 盈利标准 | 未定义 | ROI > 40% |
| 冷启动失败标准 | cost < 50元 | 前24h ROI < 10% |
| 关键决策点 | 未明确 | 72小时 |
